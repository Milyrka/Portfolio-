"use client";
import {useEffect,useRef} from "react";

export function MotionField(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext("2d");if(!x)return;let id=0,w=0,h=0,mx=.5,my=.5,px=.5,py=.5;const dots=Array.from({length:38},(_,i)=>({x:(i*97%101)/101,y:(i*43%89)/89,s:.35+(i%5)*.12}));const resize=()=>{w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+"px";c.style.height=innerHeight+"px"};const move=(e:PointerEvent)=>{mx=e.clientX/innerWidth;my=e.clientY/innerHeight};const draw=()=>{px+=(mx-px)*.045;py+=(my-py)*.045;x.clearRect(0,0,w,h);x.fillStyle="rgba(228,205,173,.22)";for(const d of dots){const dx=(px-.5)*w*d.s*.08,dy=(py-.5)*h*d.s*.08;x.beginPath();x.arc(d.x*w+dx,d.y*h+dy,Math.max(1,devicePixelRatio*d.s),0,Math.PI*2);x.fill()}id=requestAnimationFrame(draw)};resize();addEventListener("resize",resize);addEventListener("pointermove",move,{passive:true});draw();return()=>{cancelAnimationFrame(id);removeEventListener("resize",resize);removeEventListener("pointermove",move)}} ,[]);return <canvas className="motion-field" ref={ref} aria-hidden="true"/>}
